# Data

`raw/plecs/` contains the eight PLECS AC-sweep exports used by the validation
pipeline. The files are small and versioned so every published comparison can
be reproduced without rerunning proprietary simulation software.

No generated comparison table should be placed in `data/raw/`.

