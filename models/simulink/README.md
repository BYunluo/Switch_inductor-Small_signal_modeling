# Simulink Model

`switch_inductor_system.slx` is an unchanged, renamed copy of the original
system circuit and digital-control model. See `docs/simulink_model.md` for its
role, package metadata, and dependency notes.

